第一步：注册账号
       openjudge.cn
       加入小组 http://cxsjsxmooc.openjudge.cn/
第二步：下载IDE (PyCharm)
第三步：
      语法：
      1、程序中的符号必须是英文 如, . |
      2、注释使用 # 作为开头 如： #这是一个注释
      3、变量：不必说明变量类型 如： a = 12   b = "hello"
         其中，and as assert break class continue def del elif else except exec for finally from global if import in is lambda not or pass print raise return try while with yield
         Python已经预留，不可用作变量命名
      4、字符串：用单引号或双引号括起来 如： x = "I said:'hello'"   打印出来后为 I said :'hello'
                 用 + 连接字符串 如： a = "ABCD"  b = "123" c = a + b 其中c = "ABCD123"
                 用 in ，not in 判断是否字串 如： print("en" is in "english") 得到True
      5、数的转换: int(x) 可以把字符串(字符串必须由数字组成)转换成整数 float(x) str(x) eval(x):把字符串x看成一个python表达式
      6、数据类型： int float complex(复数) str list(列表) tuple(元组) bool dict(字典) set(集合)
      7、输出语句： print(x,y,z...,end="")  其中end可以缺省，默认为换行
                    h = 1.79
                    print("My name is %s,I am %.2fm tall." % ("tom",h))    得到 My name is tom,I am 1.79m tall.  
      8、输入语句： x=input()  括号中可写打印出的内容
      9、列表：列表可以有任意多的元素，可通过下标访问。
               如： list = ['Google','Rush',123]
      若x为字符串，则x.split()是一个列表，把x在包含" " "\t" "\n" 分隔开。 如： print("34\t\t45\n7".split()) 得到 ['34','45','7']  
      10、算术优先级： 第一级： **
                       第二级： -(求相反数)
                       第三级： * / // %
                       第四级： + -(减法)
      11、python中的true false应写成 True False
      12、表达或、且用 or and 如： name1 or name2  name1 and name2
      13、条件语句用 if 表达式 :   
                    elif 表达式 : 
                    else:
          if语句的语句组，每条语句左边必须缩进对齐
